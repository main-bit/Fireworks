#pragma once
#ifndef LIN_H
#define LIN_H



namespace Lin {
	const int Rand(const int _Min, const int _Max);		//·µ»ØËæ»úÊý
	//template<typename T>
	//bool opposite(T, T);
	bool opposite(double, double);
}

#endif
